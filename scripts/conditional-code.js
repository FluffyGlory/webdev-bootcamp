const myName = "max";

if (myName === "max") {
  console.log("Hello!");
}
let isLoggedIn = true;

if (!isLoggedIn) {
  console.log("User is NOT logged in!");
}

const enteredUserName = "Leif";

if (enteredUserName) {
  // using a string as a bool will return false if empty, true if full
  console.log('Input is valid!');
}
